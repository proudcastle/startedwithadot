---
phase: 03-game-canvas-and-versions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/versions/version-card.tsx
  - components/versions/version-badge.tsx
  - app/changelog/page.tsx
  - components/header.tsx
autonomous: true
requirements:
  - CHNG-01
  - CHNG-02
  - CHNG-03

must_haves:
  truths:
    - "Visiting /changelog shows all versions in reverse chronological order"
    - "Each version entry displays version number, title, date, linked proposal author, and description"
    - "When no versions exist, an empty state with narrator-voice copy appears"
    - "A version number indicator in the header links to /changelog"
    - "When no versions exist, header shows v0 with dot symbol"
  artifacts:
    - path: "app/changelog/page.tsx"
      provides: "Version history page"
      min_lines: 30
      contains: "versions"
    - path: "components/versions/version-card.tsx"
      provides: "Single version entry component"
      contains: "VersionCard"
    - path: "components/versions/version-badge.tsx"
      provides: "Header version number badge"
      contains: "VersionBadge"
    - path: "components/header.tsx"
      provides: "Updated header with version link"
      contains: "VersionBadge"
  key_links:
    - from: "app/changelog/page.tsx"
      to: "components/versions/version-card.tsx"
      via: "import and map render"
      pattern: "import.*VersionCard"
    - from: "components/header.tsx"
      to: "components/versions/version-badge.tsx"
      via: "import and render"
      pattern: "import.*VersionBadge"
    - from: "components/versions/version-badge.tsx"
      to: "/changelog"
      via: "Next.js Link href"
      pattern: 'href="/changelog"'
    - from: "app/changelog/page.tsx"
      to: "supabase.from('versions')"
      via: "Server Component data fetch"
      pattern: "from.*versions"
---

<objective>
Build the version history changelog page and add a version number indicator in the header that links to it.

Purpose: Closes the visual loop -- users can see the dot evolving through community-decided versions. The header indicator reinforces that the dot has a living history.
Output: A /changelog page showing all versions, and a "v{N}" badge in the header linking to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-canvas-and-versions/03-CONTEXT.md
@.planning/phases/03-game-canvas-and-versions/03-RESEARCH.md
@.planning/phases/03-game-canvas-and-versions/03-PATTERNS.md
@.planning/phases/03-game-canvas-and-versions/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From types/database.ts:
  versions.Row: { id: string, version_number: string, title: string, description: string | null, proposal_id: string | null, created_by: string, created_at: string }
  proposals.Row: { id: string, user_id: string, text: string, status: proposal_status, vote_count: number, created_at: string }
  profiles.Row: { id: string, username: string, is_admin: boolean, created_at: string }

From lib/supabase/server.ts:
  export async function createClient(): Promise<SupabaseClient<Database>>

From lib/format.ts:
  export function timeAgo(dateStr: string): string

From components/ui/8bit/card.tsx:
  export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }

From components/ui/8bit/badge.tsx:
  export { Badge }  (supports variant="outline")

From components/header.tsx (current nav pattern, lines 31-37):
  <nav className="flex items-center gap-4">
    <Link href="/proposals" className="font-[family-name:var(--font-press-start-2p)] text-sm text-muted-foreground hover:text-foreground">Proposals</Link>
    ...
  </nav>

Supabase query for versions with nested join (from RESEARCH.md):
  supabase.from("versions").select("*, proposals(text, profiles(username))").order("created_at", { ascending: false })

Header version query (from RESEARCH.md):
  supabase.from("versions").select("version_number").order("created_at", { ascending: false }).limit(1).maybeSingle()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VersionCard and VersionBadge components</name>
  <files>components/versions/version-card.tsx, components/versions/version-badge.tsx</files>
  <read_first>
    components/proposals/proposal-card.tsx (exact analog for Card usage pattern, null-safe profile access)
    components/proposals/status-badge.tsx (analog for Badge usage pattern)
    components/ui/8bit/card.tsx (exports: Card, CardHeader, CardContent, CardTitle, CardDescription)
    components/ui/8bit/badge.tsx (exports: Badge with variant prop)
    lib/format.ts (timeAgo signature)
    types/database.ts (versions Row type, lines 112-156)
    .planning/phases/03-game-canvas-and-versions/03-UI-SPEC.md (Copywriting Contract table, Typography table, Color section)
    .planning/phases/03-game-canvas-and-versions/03-CONTEXT.md (D-07, D-08, D-09)
  </read_first>
  <action>
    Create components/versions/version-card.tsx as a Server Component (no "use client" needed).

    Define a VersionCardProps interface accepting a version object. The type for the version should match the Supabase join result: the versions Row fields plus proposals as a nullable object containing text (string) and profiles as a nullable object containing username (string). Use the Database type from types/database.ts for the base Row, and extend with the join shape.

    Import Card, CardContent, CardHeader, CardTitle from "@/components/ui/8bit/card". Import timeAgo from "@/lib/format". Import Link from "next/link".

    Render each version as a Card. CardHeader contains CardTitle with the version number and title: "v{version.version_number}" in Press Start 2P font (font-[family-name:var(--font-press-start-2p)] text-xl), followed by a dot separator, then the title. CardContent contains: the description in text-base text-foreground (if description is not null), and a metadata row showing @{username} (null-safe: version.proposals?.profiles?.username ?? "unknown"), a dot separator, timeAgo(version.created_at), and if version.proposals?.text exists show "Proposed:" followed by the proposal text truncated to 60 chars with ellipsis per UI-SPEC Copywriting Contract.

    Create components/versions/version-badge.tsx as a Server Component. It accepts a versionNumber prop (string or null). Import Badge from "@/components/ui/8bit/badge" and Link from "next/link". Render a Link to "/changelog" wrapping a Badge with variant="outline". Content: "v{versionNumber ?? '0'}" followed by a space and the dot symbol. Font class: font-[family-name:var(--font-press-start-2p)] text-xs per UI-SPEC Typography table (Display role). Per D-09, when no versions exist, show "v0".
  </action>
  <verify>
    <automated>cd /home/proudcastle/00_ideas/startedwithadot && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - components/versions/version-card.tsx exists and exports VersionCard
    - components/versions/version-badge.tsx exists and exports VersionBadge
    - VersionCard uses 8bitcn Card, CardHeader, CardContent, CardTitle
    - VersionCard safely accesses version.proposals?.profiles?.username with ?? "unknown" fallback
    - VersionCard calls timeAgo for date display
    - VersionCard truncates proposal text to 60 chars when present
    - VersionBadge renders Badge with variant="outline" wrapped in Link to /changelog
    - VersionBadge shows "v0" when versionNumber is null
    - Both files have no "use client" directive (Server Components)
    - Build succeeds
  </acceptance_criteria>
  <done>VersionCard displays version number, title, description, author, date, and linked proposal text. VersionBadge renders a clickable version indicator linking to /changelog.</done>
</task>

<task type="auto">
  <name>Task 2: Create changelog page and add version badge to header</name>
  <files>app/changelog/page.tsx, components/header.tsx</files>
  <read_first>
    app/proposals/page.tsx (exact analog: Server Component with Supabase fetch, Suspense, empty state, page layout container)
    components/header.tsx (current file -- adding version badge to nav)
    components/versions/version-card.tsx (just created -- verify export and props)
    components/versions/version-badge.tsx (just created -- verify export and props)
    .planning/phases/03-game-canvas-and-versions/03-CONTEXT.md (D-06, D-07, D-08, D-09, D-10)
    .planning/phases/03-game-canvas-and-versions/03-UI-SPEC.md (Changelog Page layout, Copywriting Contract, Empty state)
    .planning/phases/03-game-canvas-and-versions/03-RESEARCH.md (Changelog Page Query Pattern, Header Version Number Query, Pitfall 4 and Pitfall 5)
  </read_first>
  <action>
    Create app/changelog/page.tsx as an async Server Component per D-06. Import createClient from "@/lib/supabase/server", VersionCard from "@/components/versions/version-card", and Link from "next/link".

    The default export async function ChangelogPage fetches versions from Supabase: supabase.from("versions").select("*, proposals(text, profiles(username))").order("created_at", { ascending: false }). Per RESEARCH.md open question 1, if the two-level nested join does not work, the executor should fall back to a simpler join or separate queries.

    Page wrapper: div with className "max-w-3xl mx-auto px-5 py-8" per UI-SPEC layout. Heading: h1 with text "The Evolution" in Press Start 2P font (text-xl). Subheading: p with text "Every version, decided by you." in text-sm text-muted-foreground.

    Empty state (when versions is null/empty): per D-08 and UI-SPEC Copywriting Contract, show centered block with "No versions yet." heading in Press Start 2P text-xs, body text "The dot is waiting for its first evolution. Go propose something." in text-muted-foreground, and a Link to /proposals with text "Propose an idea" styled as a visible, focusable link (text-foreground underline). The CTA link must be a real Link element per accessibility notes.

    When versions exist, map them into VersionCard components inside a div with className "space-y-6 mt-8" per UI-SPEC card gap.

    Add Next.js metadata export: title "The Evolution | startedwithadot", description "Every version of the dot, decided by the community."

    Modify components/header.tsx: import VersionBadge from "@/components/versions/version-badge". After the existing profile query block and before the return statement, add a version number query: supabase.from("versions").select("version_number").order("created_at", { ascending: false }).limit(1).maybeSingle() per D-10. Extract latestVersion?.version_number. In the nav element, add VersionBadge as the first child before the Proposals link, passing versionNumber={latestVersion?.version_number ?? null}.
  </action>
  <verify>
    <automated>cd /home/proudcastle/00_ideas/startedwithadot && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/changelog/page.tsx exists as async Server Component with Supabase fetch
    - Versions query uses .order("created_at", { ascending: false })
    - Empty state shows narrator-voice copy with "No versions yet." and a Link to /proposals
    - Each version is rendered via VersionCard component
    - Page has metadata export with title and description
    - components/header.tsx imports and renders VersionBadge before Proposals link
    - Header queries versions table with .limit(1).maybeSingle() for latest version number
    - VersionBadge receives versionNumber prop (null-safe)
    - Build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Changelog page at /changelog displays all versions reverse-chronologically with narrator empty state. Header shows version number badge linking to /changelog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase RLS -> Server Component | Versions table has SELECT for anon/authenticated (public read). No untrusted write path in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | I (Information Disclosure) | Changelog page | accept | All version data is intentionally public. No PII beyond usernames (already public in proposals feed). RLS allows SELECT for all roles. |
| T-03-03 | S (Spoofing) | Header version query | accept | Read-only query, no auth required. versions table INSERT restricted to admin via RLS (set in Phase 1). |
</threat_model>

<verification>
1. Navigate to /changelog -- page renders with heading "The Evolution" and empty state if no versions exist
2. Create a version via admin flow (Phase 2) -- it appears in the changelog with all fields
3. Header shows version badge (e.g., "v0.1") linking to /changelog
4. With no versions, header shows "v0" badge
5. Clicking the badge navigates to /changelog
6. Build completes with zero TypeScript errors
</verification>

<success_criteria>
The /changelog page displays all versions in reverse chronological order with version number, title, date, author, and description. Empty state uses narrator voice. Header shows current version number linking to /changelog.
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-canvas-and-versions/03-02-SUMMARY.md`
</output>
