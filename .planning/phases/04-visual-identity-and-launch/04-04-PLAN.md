---
phase: 04-visual-identity-and-launch
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified: []
autonomous: false
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
  - VISL-11

must_haves:
  truths:
    - "Site is deployed to Vercel and accessible at startedwithadot.com"
    - "All pages are responsive across mobile (375px) and desktop (1440px) viewports"
    - "OG tags render correctly in link preview tools"
    - "Favicon shows white dot in browser tab"
    - "Dot visual DNA is consistent: vote counters, version badge, separators, loader, favicon all use dot motif"
  artifacts: []
  key_links: []
---

<objective>
Deploy the completed site to Vercel, configure the domain, and verify responsive design and visual consistency across the entire application.

Purpose: Ship the product. Make it live and verify everything works end-to-end in production.
Output: Live site at startedwithadot.com, verified responsive and visually consistent.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-identity-and-launch/04-CONTEXT.md
@.planning/phases/04-visual-identity-and-launch/04-01-SUMMARY.md
@.planning/phases/04-visual-identity-and-launch/04-02-SUMMARY.md
@.planning/phases/04-visual-identity-and-launch/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify dot visual DNA consistency across existing components</name>
  <files></files>
  <read_first>
    components/proposals/dot-counter.tsx
    components/proposals/status-badge.tsx
    components/versions/version-badge.tsx
    components/dot-separator.tsx
    components/dot-loader.tsx
  </read_first>
  <action>
Per D-11 (VISL-01): Audit existing dot usage for visual consistency. Read the five files listed in read_first. Verify:
- DotCounter uses filled circle character for vote display
- StatusBadge uses dot prefix for status indicators
- VersionBadge uses dot symbol
- DotSeparator uses middle-dot characters
- DotLoader uses filled circle with pulse animation

Confirm the dot motif is consistent across all components. If any component uses inconsistent dot characters (e.g., bullet vs middle-dot vs filled circle), note the discrepancy but do NOT change existing working components unless the inconsistency is visually jarring. The audit is verification, not modification.

Report findings as a comment in the summary.
  </action>
  <verify>
    <automated>cd /home/proudcastle/00_ideas/startedwithadot && grep -r "●\|·\|•\|◦\|⚫\|⬤" components/ --include="*.tsx" -l</automated>
  </verify>
  <acceptance_criteria>
    - All dot-using components audited
    - Dot character usage documented (which components use which characters)
    - No visual DNA inconsistencies that need fixing, OR fixes applied if needed
  </acceptance_criteria>
  <done>Dot visual DNA audit complete, all components reviewed for consistency</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy to Vercel and configure domain</name>
  <read_first>
    .planning/phases/04-visual-identity-and-launch/04-CONTEXT.md
  </read_first>
  <what-built>The complete application with landing page, visual identity, narrator voice, SEO assets, and footer. Ready for deployment.</what-built>
  <how-to-verify>
Deploy the site to Vercel:

1. Push all code to the Git remote (GitHub repository)
2. Connect repository to Vercel (vercel.com dashboard or `npx vercel`):
   - Set environment variables:
     - `NEXT_PUBLIC_SUPABASE_URL` (from Supabase project settings)
     - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (from Supabase project settings, "anon" key)
   - Framework preset: Next.js (auto-detected)
3. Configure custom domain: startedwithadot.com
   - In Vercel dashboard: Settings -> Domains -> Add "startedwithadot.com"
   - Update DNS records as instructed by Vercel
4. Wait for deployment to complete and SSL certificate to provision

After deployment, verify:
- Visit https://startedwithadot.com and confirm landing page loads
- Check favicon in browser tab (white dot on black)
- Test OG tags: paste URL into https://www.opengraph.xyz/ or Twitter card validator
- Navigate to /nonexistent to confirm custom 404 page
- Check footer appears on all pages
  </how-to-verify>
  <resume-signal>Type "deployed" with the live URL, or describe any deployment issues</resume-signal>
  <acceptance_criteria>
    - Site is live at startedwithadot.com (or Vercel preview URL if domain not yet configured)
    - Environment variables set for Supabase connection
    - Landing page loads with all 5 sections
    - Favicon shows white dot
    - OG image renders in preview tools
  </acceptance_criteria>
  <done>Site deployed to Vercel and accessible at the configured domain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Responsive design and visual consistency verification</name>
  <read_first>
    .planning/phases/04-visual-identity-and-launch/04-UI-SPEC.md
  </read_first>
  <what-built>Complete visual identity implementation across the entire app: landing page, auth pages, proposals, changelog, 404, footer, favicon, OG image, narrator voice.</what-built>
  <how-to-verify>
Per D-17 (DEPL-03): Verify responsive design across all pages. Open Chrome DevTools (F12), toggle device toolbar, and check at these viewports:

**Mobile (375px width):**
1. Landing page: all sections stack vertically, text readable, canvas scales, CTA items stack
2. Proposals page: cards full-width, input usable
3. Changelog page: version cards full-width
4. Auth pages: forms centered and usable
5. 404 page: centered and readable
6. Footer: stacks vertically (copyright above links)

**Desktop (1440px width):**
1. Landing page: centered content with comfortable margins, CTA items in a row
2. All pages: max-width containers prevent content from stretching too wide
3. Footer: copyright and links side by side

**Visual consistency:**
1. Narrator voice: Read through auth pages, empty states, 404, footer -- does the tone feel consistent?
2. Dot DNA: Are dots visible in vote counters, version badge, separators, loader, favicon?
3. Monochrome: No unexpected colors anywhere
4. Typography: Pixel font on headings only, Geist on body text
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
  <acceptance_criteria>
    - All pages render correctly at 375px and 1440px viewports
    - No horizontal overflow or broken layouts on mobile
    - Footer visible and properly positioned on all pages
    - Narrator voice consistent across the entire app
    - Dot visual DNA present throughout
    - Monochrome color scheme maintained
  </acceptance_criteria>
  <done>Responsive design verified across mobile and desktop, visual consistency confirmed, site is launch-ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel deployment | Environment variables must be set correctly for Supabase connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | I (Information Disclosure) | Environment variables | mitigate | Only NEXT_PUBLIC_ prefixed vars exposed to client; Supabase anon key is public by design; secret keys never committed |
| T-04-07 | T (Tampering) | DNS configuration | mitigate | Use Vercel's SSL provisioning; enforce HTTPS redirect |
</threat_model>

<verification>
1. Site loads at configured domain
2. All pages responsive at 375px and 1440px
3. Favicon, OG image, metadata correct
4. Narrator voice consistent
5. Dot visual DNA present throughout
</verification>

<success_criteria>
- Site deployed and accessible at startedwithadot.com
- Responsive across mobile and desktop
- Visual identity complete and consistent
- SEO assets render correctly in preview tools
- Phase 4 complete -- V1 shipped
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-identity-and-launch/04-04-SUMMARY.md`
</output>
