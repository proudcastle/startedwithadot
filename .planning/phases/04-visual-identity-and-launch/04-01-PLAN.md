---
phase: 04-visual-identity-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dot-separator.tsx
  - components/dot-loader.tsx
  - components/footer.tsx
  - app/globals.css
  - app/not-found.tsx
  - app/layout.tsx
  - app/proposals/page.tsx
  - app/changelog/page.tsx
  - components/sign-up-form.tsx
  - components/forgot-password-form.tsx
  - components/update-password-form.tsx
autonomous: true
requirements:
  - VISL-02
  - VISL-03
  - VISL-04
  - VISL-05
  - VISL-06
  - VISL-07
  - FOOT-01
  - FOOT-02
  - SEO-01
  - DEPL-03

must_haves:
  truths:
    - "Three-dot separators render as centered muted dots throughout the app"
    - "Loading states show a pulsing dot instead of text"
    - "404 page displays narrator-voice copy with link back to home"
    - "Footer shows narrator copyright quip and links to changelog, privacy, impressum"
    - "All auth page micro-copy uses sloppy-narrator voice consistently"
  artifacts:
    - path: "components/dot-separator.tsx"
      provides: "Reusable three-dot section separator"
      exports: ["DotSeparator"]
    - path: "components/dot-loader.tsx"
      provides: "Pulsing dot loading indicator"
      exports: ["DotLoader"]
    - path: "components/footer.tsx"
      provides: "Narrator-style footer with links"
      exports: ["Footer"]
    - path: "app/not-found.tsx"
      provides: "Custom 404 page with narrator voice"
    - path: "app/globals.css"
      provides: "Custom dot-pulse keyframe animation"
      contains: "@keyframes dot-pulse"
  key_links:
    - from: "app/layout.tsx"
      to: "components/footer.tsx"
      via: "import and render below children"
      pattern: "<Footer />"
    - from: "app/proposals/page.tsx"
      to: "components/dot-loader.tsx"
      via: "Suspense fallback"
      pattern: "<DotLoader />"
    - from: "app/changelog/page.tsx"
      to: "components/dot-loader.tsx"
      via: "Suspense fallback"
      pattern: "<DotLoader />"
---

<objective>
Create the reusable visual DNA components (DotSeparator, DotLoader), Footer, 404 page, and retrofit narrator voice across all micro-copy. These components are used by the landing page (Plan 02) and establish the visual identity foundation.

Purpose: Establish the dot-as-visual-DNA pattern and narrator voice consistency before building the landing page that depends on these components.
Output: DotSeparator, DotLoader, Footer components; custom 404 page; narrator voice across all auth and system pages; DotLoader in all Suspense fallbacks.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-identity-and-launch/04-CONTEXT.md
@.planning/phases/04-visual-identity-and-launch/04-RESEARCH.md
@.planning/phases/04-visual-identity-and-launch/04-PATTERNS.md
@.planning/phases/04-visual-identity-and-launch/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From components/proposals/proposal-card.tsx:
```typescript
interface ProposalCardProps {
  proposal: {
    id: string;
    text: string;
    status: ProposalStatus;
    vote_count: number;
    created_at: string;
    profiles: { username: string } | null;
  };
  children?: ReactNode;
  actions?: ReactNode;
}
export function ProposalCard({ proposal, children, actions }: ProposalCardProps)
```

From components/header.tsx (layout pattern):
```typescript
// w-full bg-card border-b border-border h-16 flex items-center
// max-w-5xl mx-auto flex justify-between items-center px-5
// text-muted-foreground hover:text-foreground for links
// font-[family-name:var(--font-press-start-2p)] for pixel text
```

From app/layout.tsx (insertion point for Footer):
```typescript
<Suspense>
  <Header />
</Suspense>
{children}
{/* Footer goes here, before Toaster */}
<Toaster />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable visual DNA components, custom dot-pulse animation, and 404 page</name>
  <files>components/dot-separator.tsx, components/dot-loader.tsx, app/globals.css, app/not-found.tsx</files>
  <read_first>
    app/globals.css
    app/auth/error/page.tsx
    components/proposals/dot-counter.tsx
  </read_first>
  <action>
Create four files:

1. `components/dot-separator.tsx` -- per D-02, D-10. Pure presentational Server Component (no "use client" directive). Renders three middle-dot characters (middle dot entity, not bullet) centered horizontally with `text-muted-foreground`, `tracking-[0.5em]`, `py-8`, and `aria-hidden="true"` since it is decorative. No props needed. Export named `DotSeparator`.

2. `components/dot-loader.tsx` -- per D-09 (VISL-06). Pure presentational component. Renders a centered container with a small white dot (`h-4 w-4 rounded-full bg-foreground`) using a custom `animate-[dot-pulse_1.5s_ease-in-out_infinite]` class that references the keyframe added in globals.css. The dot-pulse keyframe scales from 1 to 1.3 and back with opacity shift (1 to 0.7). Wrap in `flex items-center justify-center py-12`. Export named `DotLoader`.

3. `app/globals.css` -- Add the `@keyframes dot-pulse` rule after the existing body rule block (after line 88). The keyframe: 0%,100% scale(1) opacity(1); 50% scale(1.3) opacity(0.7). This mirrors the canvas pulse feel from game-canvas.tsx.

4. `app/not-found.tsx` -- per D-06 (VISL-05). Default export function `NotFound`. Centered layout using `min-h-[60vh] flex flex-col items-center justify-center p-6`. Display "404" as heading with `font-[family-name:var(--font-press-start-2p)] text-4xl mb-4`. Body text: "This page doesn't exist. Much like this game's roadmap." in `text-muted-foreground mb-6`. Link back to "/" with text "Back to the dot" styled as `text-foreground underline underline-offset-4`. Import Link from "next/link". No Card wrapper needed -- simpler than the auth error page pattern.
  </action>
  <verify>
    <automated>cd /home/proudcastle/00_ideas/startedwithadot && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DotSeparator renders three middle-dot characters centered with muted color and aria-hidden
    - DotLoader renders a pulsing white dot using CSS keyframe animation (not Tailwind animate-pulse)
    - globals.css contains @keyframes dot-pulse with scale and opacity transitions
    - not-found.tsx exports default function with 404 heading, narrator body text, and link to "/"
    - Build completes without errors
  </acceptance_criteria>
  <done>All four files exist, export correctly, and the project builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add Footer to layout, update metadata, replace Suspense fallbacks with DotLoader, and retrofit narrator voice</name>
  <files>components/footer.tsx, app/layout.tsx, app/proposals/page.tsx, app/changelog/page.tsx, components/sign-up-form.tsx, components/forgot-password-form.tsx, components/update-password-form.tsx</files>
  <read_first>
    app/layout.tsx
    app/proposals/page.tsx
    app/changelog/page.tsx
    components/sign-up-form.tsx
    components/forgot-password-form.tsx
    components/update-password-form.tsx
    components/login-form.tsx
  </read_first>
  <action>
1. `components/footer.tsx` -- per D-15 (FOOT-01, FOOT-02). Server Component (no "use client"). Import Link from "next/link". Render a `footer` element with `w-full border-t border-border py-8 mt-auto`. Inner container: `max-w-5xl mx-auto px-5 flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-muted-foreground`. Left side: copyright text from UI-SPEC: "Made by nobody. Shaped by everybody." preceded by year (use template). Right side: nav with `flex gap-4` containing Links to /changelog ("Changelog"), /privacy ("Privacy"), /impressum ("Impressum") with `hover:text-foreground` styling. Export named `Footer`.

2. `app/layout.tsx` -- Two changes:
   a. Update metadata: change description to "It's a dot. It does nothing. You decide what happens next." per D-13 (SEO-01). Add openGraph and twitter objects per the pattern in RESEARCH.md. Keep metadataBase and title as-is.
   b. Import and render `<Footer />` between `{children}` and `<Toaster />`. Use a flex layout on body: add `min-h-svh flex flex-col` to the body className. Wrap `{children}` in a `<div className="flex-1">` element (use a div, NOT a semantic main element -- each page component owns its own semantic main tag to avoid nested main elements in the HTML). This pushes Footer to the bottom on short pages.

3. `app/proposals/page.tsx` -- Import DotLoader from "@/components/dot-loader". Replace the existing Suspense fallback `<div className="text-center py-12 text-muted-foreground">Loading proposals...</div>` with `<DotLoader />`. Per VISL-06.

4. `app/changelog/page.tsx` -- Same as above: import DotLoader, replace "Loading versions..." Suspense fallback with `<DotLoader />`. Per VISL-06.

5. Narrator voice audit per D-05, D-07 (VISL-02, VISL-04). Read login-form.tsx first to confirm it already has correct narrator copy (it does per research audit -- "Welcome back" / "The dot missed you."). Then update:
   - `components/sign-up-form.tsx` -- verify heading says "Join the experiment" and subtext says "Pick a username. It's permanent. Choose wisely." per UI-SPEC copy contract. Update if different.
   - `components/forgot-password-form.tsx` -- verify/update heading to "Forgot your password?" and subtext to "It happens. Even to dots." per UI-SPEC.
   - `components/update-password-form.tsx` -- verify/update heading to "New password" and subtext to "Make it a good one. The dot is watching." per UI-SPEC.

Do NOT modify login-form.tsx if it already matches (research audit confirms it does). Only edit files where copy differs from the UI-SPEC copywriting contract.

6. Create placeholder pages for privacy and impressum (Footer links need destinations):
   - `app/privacy/page.tsx` -- Simple page with heading "Privacy" in pixel font and body text in narrator voice: "This page exists because lawyers exist. We collect your email and your opinions about a dot. That's it." Link back to home.
   - `app/impressum/page.tsx` -- Simple page with heading "Impressum" in pixel font and body text: "Legally required disclosure: someone made this. A dot was involved." Link back to home.
  </action>
  <verify>
    <automated>cd /home/proudcastle/00_ideas/startedwithadot && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Footer component renders with narrator copyright and links to changelog, privacy, impressum
    - Footer appears at bottom of every page via root layout
    - Layout metadata has updated description and openGraph/twitter objects per SEO-01
    - Body has min-h-svh flex flex-col so Footer is pushed to bottom on short pages
    - Children wrapped in div (not main) with flex-1 to avoid nested main elements
    - Proposals page Suspense fallback uses DotLoader (no "Loading proposals..." text)
    - Changelog page Suspense fallback uses DotLoader (no "Loading versions..." text)
    - Auth form copy matches UI-SPEC copywriting contract
    - Privacy and impressum placeholder pages exist and render
    - Build completes without errors
  </acceptance_criteria>
  <done>Footer in layout, metadata updated, all Suspense fallbacks use DotLoader, narrator voice consistent across auth pages, privacy/impressum placeholder pages exist, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan is purely presentational -- no user input, no data mutations, no auth changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information Disclosure) | OG metadata | accept | Public metadata by design -- title and description are intentionally public |
| T-04-02 | S (Spoofing) | Privacy/Impressum pages | accept | Placeholder pages have no forms or user input -- no spoofing surface |
</threat_model>

<verification>
1. `npx next build` completes without errors
2. Navigate to `/nonexistent-path` and see custom 404 with narrator copy
3. DotSeparator and DotLoader render correctly when imported
4. Footer visible at bottom of every page
5. Auth pages show narrator voice copy matching UI-SPEC contract
</verification>

<success_criteria>
- All reusable visual DNA components (DotSeparator, DotLoader) created and functional
- Footer renders on every page with narrator quip and navigation links
- 404 page has narrator voice
- All Suspense fallbacks use DotLoader
- Narrator voice consistent across all auth and system pages
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-identity-and-launch/04-01-SUMMARY.md`
</output>
