---
phase: 04-visual-identity-and-launch
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/page.tsx
autonomous: true
requirements:
  - VISL-01
  - VISL-07
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05

must_haves:
  truths:
    - "Landing page hero shows GameCanvas with narrator primary and secondary text"
    - "Story section explains the concept in sloppy-narrator voice"
    - "Three-step CTA shows Sign up / Propose / Vote with dot bullets and links"
    - "Proposals preview shows top 5 proposals by votes (read-only, no actions)"
    - "Changelog preview shows 3 most recent versions"
    - "DotSeparators divide each section"
  artifacts:
    - path: "app/page.tsx"
      provides: "Full landing page with 5 sections"
      min_lines: 80
  key_links:
    - from: "app/page.tsx"
      to: "components/game/game-canvas.tsx"
      via: "import for hero section"
      pattern: "GameCanvas"
    - from: "app/page.tsx"
      to: "components/dot-separator.tsx"
      via: "import for section dividers"
      pattern: "DotSeparator"
    - from: "app/page.tsx"
      to: "components/dot-loader.tsx"
      via: "Suspense fallback for data sections"
      pattern: "DotLoader"
    - from: "app/page.tsx"
      to: "lib/supabase/server.ts"
      via: "createClient for proposals and versions queries"
      pattern: "createClient"
    - from: "app/page.tsx"
      to: "components/proposals/proposal-card.tsx"
      via: "import for proposals preview"
      pattern: "ProposalCard"
    - from: "app/page.tsx"
      to: "components/versions/version-card.tsx"
      via: "import for changelog preview"
      pattern: "VersionCard"
---

<objective>
Build the full landing page by expanding app/page.tsx from a stub into a five-section manifesto page with hero, story, CTA, proposals preview, and changelog preview.

Purpose: The landing page is the first thing visitors see. It must communicate the concept, invite participation, and showcase community activity.
Output: Complete landing page at app/page.tsx with all five sections, data fetching, and Suspense boundaries.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-identity-and-launch/04-CONTEXT.md
@.planning/phases/04-visual-identity-and-launch/04-RESEARCH.md
@.planning/phases/04-visual-identity-and-launch/04-PATTERNS.md
@.planning/phases/04-visual-identity-and-launch/04-UI-SPEC.md
@.planning/phases/04-visual-identity-and-launch/04-01-SUMMARY.md

<interfaces>
<!-- Components available from Plan 01 and prior phases -->

From components/dot-separator.tsx (created in Plan 01):
```typescript
export function DotSeparator(): JSX.Element
// Renders "· · ·" centered, muted color, aria-hidden
```

From components/dot-loader.tsx (created in Plan 01):
```typescript
export function DotLoader(): JSX.Element
// Renders pulsing dot for Suspense fallbacks
```

From components/game/game-canvas.tsx:
```typescript
"use client";
export function GameCanvas(): JSX.Element
// Full-width canvas with pulsing white dot on black bg
// Container: minHeight 60vh
```

From components/proposals/proposal-card.tsx:
```typescript
interface ProposalCardProps {
  proposal: {
    id: string;
    text: string;
    status: ProposalStatus;
    vote_count: number;
    created_at: string;
    profiles: { username: string } | null;
  };
  children?: ReactNode;
  actions?: ReactNode;
}
export function ProposalCard({ proposal, children, actions }: ProposalCardProps)
```

From components/versions/version-card.tsx:
```typescript
export interface VersionWithProposal extends VersionRow {
  proposals: {
    text: string;
    profiles: { username: string } | null;
  } | null;
}
export function VersionCard({ version }: VersionCardProps)
```

From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

Established Supabase query patterns (from proposals/page.tsx, changelog/page.tsx):
```typescript
// Proposals: .from("proposals").select("*, profiles(username)").order("vote_count", { ascending: false }).limit(5)
// Versions: .from("versions").select("*, proposals(text, profiles(username))").order("created_at", { ascending: false }).limit(3)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the complete landing page with five sections and data fetching</name>
  <files>app/page.tsx</files>
  <read_first>
    app/page.tsx
    app/changelog/page.tsx
    app/proposals/page.tsx
    components/proposals/proposal-card.tsx
    components/versions/version-card.tsx
    components/game/game-canvas.tsx
  </read_first>
  <action>
Rewrite `app/page.tsx` entirely per D-01 (LAND-01 through LAND-05). The page is a Server Component (no "use client"). It contains five sections separated by DotSeparator, with two async data-fetching components wrapped in Suspense.

**Imports needed:** Suspense from "react", Link from "next/link", GameCanvas from "@/components/game/game-canvas", DotSeparator from "@/components/dot-separator", DotLoader from "@/components/dot-loader", createClient from "@/lib/supabase/server", ProposalCard from "@/components/proposals/proposal-card", VersionCard and VersionWithProposal type from "@/components/versions/version-card".

**Page structure (default export `Home`, synchronous function):**

**Section 1: Hero (LAND-01)**
- GameCanvas rendered first (full-width, it has its own minHeight)
- Below canvas: centered text container (`max-w-3xl mx-auto px-5 py-12 text-center`)
- Primary text: "It all started with a dot." in `font-[family-name:var(--font-press-start-2p)] text-xl leading-relaxed`
- Secondary text: "It does nothing. You decide what happens next." in `text-muted-foreground mt-4 text-lg`

**DotSeparator**

**Section 2: Story (LAND-02)**
- Container: `max-w-3xl mx-auto px-5 py-12`
- Heading: "The Story" in `font-[family-name:var(--font-press-start-2p)] text-sm mb-6`
- Body paragraph: Use the exact copy from the UI-SPEC copywriting contract: "There's a dot. It sits there. It pulses. That's it. But here's the thing -- you get to decide what happens to it. Propose an idea. Vote on someone else's. The best ones get built. Every version of this game exists because someone like you said 'hey, what if the dot did this?'" in `text-muted-foreground leading-relaxed`

**DotSeparator**

**Section 3: Three-Step CTA (LAND-03)**
- Container: `max-w-3xl mx-auto px-5 py-12 text-center`
- Heading: "How it works" in pixel font, `text-sm mb-8`
- Three items in a `flex flex-col sm:flex-row justify-center gap-8 sm:gap-12` layout. Each item:
  - Number and label: "1. Sign up" / "2. Propose" / "3. Vote" in `text-lg`
  - Each prefixed with a dot bullet: "● " before the text
  - "Sign up" links to `/auth/sign-up`, "Propose" links to `/proposals`, "Vote" links to `/proposals`
  - Links styled as `text-foreground hover:text-muted-foreground`
- Below items: subtext "That's it. No tutorials. No onboarding. Just opinions." in `text-muted-foreground mt-8 text-sm`

**DotSeparator**

**Section 4: Proposals Preview (LAND-04)**
- Wrap in `<Suspense fallback={<DotLoader />}>`
- Define `async function ProposalsPreview()` inline in the file (or above the page component). This is an async Server Component.
- Inside: `const supabase = await createClient()`, query proposals with `.from("proposals").select("*, profiles(username)").neq("status", "rejected").order("vote_count", { ascending: false }).limit(5)`
- Container: `max-w-3xl mx-auto px-5 py-12`
- Heading: "Alright, what should happen next?" in pixel font, `text-sm mb-6` per D-03
- If proposals exist: render each in `ProposalCard` without the `actions` prop (read-only preview -- no vote buttons). Wrap in `flex flex-col gap-4`.
- If no proposals: empty state per UI-SPEC: "Nobody's proposed anything yet. Be the first to tell the dot what to do." in `text-muted-foreground`
- Below cards: centered link "See all proposals" to `/proposals` styled as `text-muted-foreground hover:text-foreground underline underline-offset-4 mt-6 inline-block`

**DotSeparator**

**Section 5: Changelog Preview (LAND-05)**
- Wrap in `<Suspense fallback={<DotLoader />}>`
- Define `async function ChangelogPreview()` inline. Async Server Component.
- Inside: `const supabase = await createClient()`, query versions with `.from("versions").select("*, proposals(text, profiles(username))").order("created_at", { ascending: false }).limit(3)`
- Container: `max-w-3xl mx-auto px-5 py-12`
- Heading: "The Evolution" in pixel font, `text-sm mb-6` per D-04
- If versions exist: render each with `VersionCard`. Cast the version data to `VersionWithProposal` type matching the component's expected interface. Wrap in `space-y-6`.
- If no versions: empty state per UI-SPEC: "Nothing's happened yet. The dot is still just a dot." in `text-muted-foreground`
- Below cards: centered link "Full changelog" to `/changelog` with same link styling as proposals CTA

The entire page is wrapped in a `main` element. Each section uses a `section` element for semantic HTML.

ProposalCard rendering note: Pass proposal data shaped to match ProposalCardProps. The `profiles` field from the Supabase query returns as `{ username: string } | null`. Cast as needed. Omit the `actions` prop entirely so no vote buttons appear. The `children` prop can also be omitted.
  </action>
  <verify>
    <automated>cd /home/proudcastle/00_ideas/startedwithadot && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Landing page has 5 distinct sections: Hero, Story, CTA, Proposals Preview, Changelog Preview
    - GameCanvas renders in the hero section
    - DotSeparators appear between every section
    - ProposalsPreview fetches top 5 non-rejected proposals by vote_count (read-only, no actions slot)
    - ChangelogPreview fetches 3 most recent versions
    - Both data-fetching components wrapped in Suspense with DotLoader fallback
    - All copy matches UI-SPEC copywriting contract
    - Empty states have narrator-voice copy
    - "See all proposals" and "Full changelog" links navigate correctly
    - Three-step CTA links to /auth/sign-up and /proposals
    - Build completes without errors
  </acceptance_criteria>
  <done>Full landing page renders with all 5 sections, data fetching works, copy matches UI-SPEC, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase RLS | Proposals and versions queries go through existing RLS policies (read-only, public SELECT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | I (Information Disclosure) | ProposalsPreview query | accept | Only shows non-rejected proposals; RLS policies enforce public SELECT access -- by design |
| T-04-04 | D (Denial of Service) | Landing page data fetching | accept | Supabase query is limited to 5/3 rows; Suspense prevents blocking; PPR-eligible via Server Components |
</threat_model>

<verification>
1. `npx next build` completes without errors
2. Landing page at "/" shows all 5 sections
3. Proposals preview shows cards (when data exists) without vote buttons
4. Changelog preview shows version cards (when data exists)
5. All links navigate to correct destinations
</verification>

<success_criteria>
- Full landing page renders at "/" with hero, story, CTA, proposals preview, changelog preview
- Data-fetching sections use Suspense with DotLoader
- Read-only proposal cards (no voting actions)
- All section copy matches UI-SPEC contract
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-identity-and-launch/04-02-SUMMARY.md`
</output>
